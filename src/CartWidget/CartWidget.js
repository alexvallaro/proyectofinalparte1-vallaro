import "./CartWidget.css"

const CartWidget = () => {
    return (
        <div className="cart">
        <i className="bi bi-cart"></i>
        <span className="cantCart">2</span>
        </div>
    )
}

export default CartWidget;